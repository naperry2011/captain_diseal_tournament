# Connection string for the Postgres database.
# In production this is provided by Vercel Postgres.
# For local development, point it at a local Postgres instance, e.g.:
#   postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/cdt?schema=public"
